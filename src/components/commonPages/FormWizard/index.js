"use client";
import { WelcomeMessageDialog } from "@/components/common/welcome-message-dialog";
import React, { useState } from "react";

const FormWizard = () => {
  return <WelcomeMessageDialog open={true} />;
};

export default FormWizard;
